{
  "name": "claude-api",
  "version": "1.0.0",
  "description": "Unofficial REST API server for Claude.ai via browser automation",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "setup": "npm install && npx playwright install chromium && npx playwright install-deps chromium",
    "postinstall": "npx playwright install chromium"
  },
  "keywords": ["claude", "api", "automation", "playwright"],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "dotenv": "^17.3.1",
    "express": "^5.2.1",
    "playwright": "^1.58.2",
    "swagger-ui-express": "^5.0.1",
    "uuid": "^13.0.0"
  }
}
